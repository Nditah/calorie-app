import { Injectable } from '@angular/core';

import { Item } from '../../../../../ionic Sample/Ionic3-Multi-Purpose-Theme/src/models/item';
import { Api } from '../api/api';

@Injectable()
export class Items {

  constructor(public api: Api) { }

  query(params?: any) {
    return this.api.get('/items', params);
  }

  add(item: Item) {
  }

  delete(item: Item) {
  }

}
